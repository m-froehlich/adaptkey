package de.froehlichmedia.adaptkey.keyboard

/**
 * Which of the keyboard's input surfaces is currently shown (L-03): the base letter layout, the
 * numeric/symbol layer (?123), or the emoji panel.
 */
enum class InputSurface {
    LETTERS,
    SYMBOLS,
    EMOJI
}
