<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/touch_model_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- D-80: generous top margin (on top of the status-bar/cutout inset padding on the root) plus a
         larger size, so the short intro reads as two clearly separated paragraphs spread over the space
         above the keyboard, instead of two cramped default-size lines hugging the very top of the screen. -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginTop="40dp"
        android:textSize="20sp"
        android:lineSpacingExtra="4dp"
        android:text="@string/d24_intro_1" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginTop="20dp"
        android:textSize="20sp"
        android:lineSpacingExtra="4dp"
        android:text="@string/d24_intro_2" />

    <TextView
        android:id="@+id/touch_model_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginTop="20dp"
        android:textSize="18sp"
        android:textStyle="italic"
        android:visibility="gone"
        android:text="@string/d24_empty" />

    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <de.froehlichmedia.adaptkey.keyboard.AdaptKeyboardView
        android:id="@+id/touch_model_keyboard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp" />

</LinearLayout>
