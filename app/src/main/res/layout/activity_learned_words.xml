<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/learned_words_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- D-404-followup: language and sort filters side by side, not stacked - both fit comfortably on one
         row, and stacking them wasted vertical space this screen has no real need to spend.
         D-404-followup (v2): no more "Sprache:"/"Sortierung:" labels - they left too little width for the
         spinners' own selected-value text, which was getting cut off; a spinner's own content already makes
         its purpose obvious without one. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <Spinner
            android:id="@+id/learned_words_language_spinner"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1" />

        <Spinner
            android:id="@+id/learned_words_sort_spinner"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_weight="1" />

    </LinearLayout>

    <TextView
        android:id="@+id/learned_words_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/learned_words_empty"
        android:visibility="gone" />

    <ListView
        android:id="@+id/learned_words_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="8dp" />

</LinearLayout>
